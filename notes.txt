variables
    In javascript variables are used to store the data that can be referenced and manupulated with in the program
    A variables act as container for the value

    In javascript there are three ways to declare the variables

    var
    let
    const     
    
     HTML:
     Hypertext Markup Language used to create and design the structure of the webpage.
     It will describe tthe content and layout of the webpage by defining the elements

     //why use HTML
      to structure and to organize the content of the page
      html provide the framework for the organizing the content of the page.
      it allows us to define the structure of the page like adding the images, text, tables, input elements


      //HTML (sematic)  --- used for the SEO search engine Optimization


    
